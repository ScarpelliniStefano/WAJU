const icons = Object.freeze({
    SEND: Object.freeze({
        BTN_EXECUTE: 'mdi-play'
    }),
    RECEIVE: Object.freeze({
        
    }),
    CONFIG_COLL: Object.freeze({
        BTN_CONFIG: 'mdi-wrench',
        BTN_IR: 'mdi-file-find-outline',
        BTN_UPLOAD: 'mdi-upload',
        LIST_IR_ITEM: 'mdi-database',
        BTN_TEMP_COLLECTION: 'mdi-page-last'
    })
});

export default icons